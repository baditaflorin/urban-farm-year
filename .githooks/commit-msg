#!/bin/sh
set -eu

msg_file="$1"
first_line="$(head -n 1 "$msg_file")"

case "$first_line" in
  feat:\ *|fix:\ *|docs:\ *|chore:\ *|refactor:\ *|test:\ *|ops:\ *|data:\ *)
    exit 0
    ;;
  *)
    printf "%s\n" "Commit message must use Conventional Commits." >&2
    exit 1
    ;;
esac
